const num1 = 2000
const num2 = 100

/* //why doesn't this work..?
const divide = (num1, num2) => num1 / num2; 
*/
const divide = () => 2000 / 100;

const square = (num1) => {
    return num1**2;
}

const add = (num1, num2) => {
    return num1 + num2;
}